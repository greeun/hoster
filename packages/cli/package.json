{
  "name": "@hoster/cli",
  "private": true,
  "type": "module",
  "bin": { "hoster": "dist/index.js" },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json && rm -rf dist/templates && cp -r ../../templates dist/templates"
  },
  "dependencies": { "commander": "^12.1.0" },
  "devDependencies": { "@types/node": "^22.0.0" }
}
