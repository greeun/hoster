{
  "name": "hoster-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "scripts": { "test": "pnpm -r test", "build": "pnpm -r build" },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0" }
}
