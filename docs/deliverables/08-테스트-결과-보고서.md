# 08. 테스트 결과 보고서

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-08-13 |
| 버전 | 1.0 |
| 실행 명령 | `pnpm -r test` (workspace root) |
| 러너 | vitest v2.1.9 |

## 2. 요약

전체 테스트: **167개, 167개 통과, 실패 0**

| 패키지 | 테스트 파일 수 | 테스트 수 | 결과 | Duration |
|---|---|---|---|---|
| `@hoster/deployer` | 5 | 46 | 전부 통과 | 2.71s |
| `@hoster/cli` | 10 | 121 | 전부 통과 | 685ms |

### 2.1 파일별 테스트 수

| 패키지 | 파일 | 테스트 수 |
|---|---|---|
| deployer | `test/hmac.test.ts` | 5 |
| deployer | `test/store.test.ts` | 4 |
| deployer | `test/app.test.ts` | 15 |
| deployer | `test/orchestrator.test.ts` | 11 |
| deployer | `test/docker.test.ts` | 11 |
| cli | `test/config.test.ts` | 3 |
| cli | `test/repo.test.ts` | 18 |
| cli | `test/cloudflare.test.ts` | 11 |
| cli | `test/nas.test.ts` | 4 |
| cli | `test/init.test.ts` | 13 |
| cli | `test/client.test.ts` | 6 |
| cli | `test/shell.test.ts` | 7 |
| cli | `test/runInit.test.ts` | 16 |
| cli | `test/runAdd.test.ts` | 17 |
| cli | `test/ops.test.ts` | 26 |

## 3. 테스트 유형

| 유형 | 설명 | 해당 파일 |
|---|---|---|
| 단위 테스트 | 순수 함수(I/O 없음) 검증 | `hmac.test.ts`, `shell.test.ts`, `repo.test.ts`(단, `shQuote` 검증은 실제 `sh -c` 실행 포함), `config.test.ts` |
| 통합 테스트(stateful fake dockerode) | 실제 `DockerManager` + 컨테이너 상태(running/image)를 추적하는 fake dockerode로 rename/stop/start/304 상호작용을 코드 경로 그대로 재현 | `orchestrator.test.ts:29-84`(`makeFakeDockerode`), `orchestrator.test.ts:257-279`("[통합]" 태그) |
| 계약(contract) 테스트 | CLI가 만든 서명을 deployer의 `verifySignature`로 직접 검증(모듈 경계를 넘어 실제 왕복) | `client.test.ts:40-92` |
| API 계층 테스트 | 실제 `Hono` 앱(`app.request()`)에 서명된 요청을 보내 라우팅·검증·응답까지 검증 | `app.test.ts` |
| 오케스트레이션 테스트 | 러너/파일시스템/네트워크 클라이언트를 의존성 주입으로 대체해 `runInit`/`runAdd`의 단계별 흐름 검증 | `runInit.test.ts`, `runAdd.test.ts`, `ops.test.ts` |
| 실daemon 검증 | 코드베이스에 실제 Docker 데몬·SSH·gh CLI를 사용하는 자동화 테스트는 없음(`docker.test.ts`는 dockerode를 `vi.fn()`으로 목 처리, `nas.test.ts`/`runAdd.test.ts`는 러너 주입) — 실daemon 대상 재현·검증은 리뷰 과정에서 수행(예: `420281e` 커밋 메시지가 언급하는 "reviewer's stopAndRemove THREW: 304" 시나리오) |

## 4. 파일별 커버 범위

| 테스트 파일 | 대상 모듈 | 주요 시나리오 |
|---|---|---|
| `deployer/test/hmac.test.ts` | `src/hmac.ts` | 서명 생성/검증, 본문 변조 실패, 시크릿 불일치 실패, 5분 초과 재전송 방지, 서명 길이 불일치 시 예외 없이 false |
| `deployer/test/store.test.ts` | `src/store.ts` | 프로젝트 upsert/get/list, 이미지 현재/직전 갱신, 배포 이력 기록/상태 갱신/최신순 조회, 프로젝트 삭제 |
| `deployer/test/app.test.ts` | `src/app.ts` | 서명 없으면 401, `/healthz` 인증 예외, 라우트별 필드 검증(project/domain/env key·value), 경로 순회 400, JSON 파싱 실패 400, env 파일 0600(신규/기존 파일 덮어쓰기 양쪽) |
| `deployer/test/orchestrator.test.ts` | `src/orchestrator.ts`(+ `docker.ts` 통합) | 배포 성공/헬스체크 실패 롤백, stop() non-404 에러 시 롤백 지속, 동시 deploy 큐(latest-wins), 락 해제, rollback 직렬화, rollback 중 non-404 에러 처리, 304 크래시 시나리오 통합 재현 |
| `deployer/test/docker.test.ts` | `src/docker.ts` | Traefik 라벨/네트워크 지정 컨테이너 생성, stopAndRemove 404/304 무시, stop/start 개별 동작과 304 무시, non-404/304 에러 전파, logs 멀티플렉스 헤더 제거, healthCheck 성공/timeout |
| `cli/test/client.test.ts` | `src/client.ts`, `src/hmac.ts` | HMAC 헤더 부착, GET 빈 body 서명, 비2xx 에러, logs 텍스트 반환, round-trip(POST/GET) 서명 검증 |
| `cli/test/config.test.ts` | `src/config.ts` | save/load 왕복, 파일 없음 안내 에러, 기존 0644 파일 덮어써도 0600 강제 |
| `cli/test/repo.test.ts` | `src/repo.ts` | ssh/https 원격 URL 파싱, GitHub 문자 집합 밖 거부, 프로젝트명 정규화(길이 상한/하이픈 경계), 브랜치명 안전 부분집합 검증, GHCR 소문자 규칙 |
| `cli/test/cloudflare.test.ts` | `src/cloudflare.ts` | 터널 생성+토큰 조회, 재사용 경로, DNS upsert(POST/PUT 분기), exact name 필터로 페이지네이션 회피, deleteDnsRecord type 필터, API 토큰 비노출, 비JSON 에러 응답 처리 |
| `cli/test/nas.test.ts` | `src/nas.ts` | ssh 인자 구성, docker sudo 절대경로 프리픽스, 비0 종료 에러, tar 비latin1 바이트 무손상 전달 |
| `cli/test/init.test.ts` | `src/commands/init.ts`(`planInit`) | 필수 액션 순서, write-config가 healthz-check보다 먼저, 네트워크 생성이 compose up보다 먼저, sudo 절대경로 일관성, 시크릿 플레이스홀더만 존재, .env chmod 600, NAS 정보 shQuote 인용 |
| `cli/test/client.test.ts` 외 `shell.test.ts` | `src/shell.ts` | shQuote 이스케이프, 악성 값이 실제 `sh -c` 실행에서 별도 명령으로 실행되지 않음을 증명, substitutePlaceholders의 `$` 오염 방지 |
| `cli/test/runInit.test.ts` | `src/commands/init.ts`(`runInit`) | dry-run, 시크릿 실치환 및 미노출, nas-transfer 정리, write-config, 네트워크 존재 무시, 진단 실패 경고만, healthz 재시도/최종 실패, 터널 이름 충돌 안내, reuseTunnelId, 부분 실패 복구 안내 |
| `cli/test/runAdd.test.ts` | `src/commands/add.ts`(`runAdd`) | dry-run, gh 미설치/미인증 시 파일 미생성, Dockerfile/workflow 생성(Next.js 감지), --project 지정, 기존 workflow 보호(--force), branch/project 검증(파일 쓰기 전), DNS/등록 부분 실패 안내 |
| `cli/test/ops.test.ts` | `src/commands/ops.ts` | ls/status/logs/rollback/env(set·rm·redeploy)/remove(도메인 조회 기반 DNS 삭제)/doctor(진단 실패는 경고, precheck 실패는 throw) |

## 5. 중점 검증 항목

| 항목 | 근거 테스트 |
|---|---|
| 동시성 — 큐 latest-wins | `orchestrator.test.ts:172-187` "동시 deploy: 진행 중 1건 + 대기 최신 1건만, 중간 요청 skipped" |
| 동시성 — rollback 직렬화 | `orchestrator.test.ts:205-238` "rollback이 진행 중인 deploy와 겹치지 않고 순서대로 직렬 실행됨" |
| 동시성 — 락 해제 | `orchestrator.test.ts:189-203` "recordDeployment 실패 시 failed 반환, 락은 해제되어 다음 배포는 정상 진행" |
| 보안 — 경로 순회 400 | `app.test.ts:102-109` "경로 순회 시도 시 400, envDir 밖에 파일을 쓰지 않음" |
| 보안 — 주입 거부 | `app.test.ts:111-117`(env 키/개행 값), `app.test.ts:124-128`(domain 특수문자), `repo.test.ts:104-118`(브랜치명 메타문자), `shell.test.ts:20-32`(shQuote 인젝션 실패 증명, 실제 `sh -c` 실행) |
| 보안 — 0600 | `app.test.ts:130-150`(env 파일 신규/덮어쓰기), `config.test.ts:26`(config.json 덮어쓰기), `init.test.ts:93-98`(NAS .env chmod 600 커맨드 포함) |
| 롤백 — 304 관용 | `docker.test.ts:62-69`(stopAndRemove 304), `docker.test.ts:97-103`(start 304) |
| 롤백 — rename+start 복구 | `orchestrator.test.ts:112-125`(헬스체크 실패), `orchestrator.test.ts:130-145`(stop() non-404 에러), `orchestrator.test.ts:257-279`([통합] 304 크래시 시나리오) |
| 계약 — CLI-deployer HMAC 왕복 | `client.test.ts:40-92` (POST/GET 양쪽, `../../deployer/src/hmac.js`를 직접 import해 검증) |
| 계약 — 비ASCII 보존 | `client.test.ts:54` (`한글 값 テスト 🚀` 포함 body가 손상 없이 서명·검증됨) |

## 6. 결함 발견·수정 이력

발견 경로 분류 근거: "태스크 리뷰"는 해당 기능의 `feat` 커밋 직후 동일 모듈에 대해 이어진 `fix` 커밋. "최종 리뷰(fix-wave)"는 `b72a15d` 커밋 메시지("The fix wave reordered planInit() ... and changed swapContainer ...")가 명시적으로 지목하는 통합 검증 라운드.

| 결함 | 발견 경로 | 수정 커밋 |
|---|---|---|
| `/env/:project` 경로 순회(hono `decodeURIComponent`로 `%2F`→`/`) | 태스크 리뷰(`d7b67d8` Hono API 직후) | `45c2e2f` |
| env 키/값 개행 주입, domain 미검증 Traefik 라벨 주입 | 태스크 리뷰(`d7b67d8` 직후) | `45c2e2f` |
| env 파일 0644 생성/유지 | 태스크 리뷰(`d7b67d8` 직후, 보완) | `45c2e2f`, `02d84cd` |
| 오케스트레이터 락 영구 고착, rollback 큐 우회, unhandled rejection | 태스크 리뷰(`112b258` 배포 오케스트레이터 직후) | `2eba716` |
| NAS 접속정보(host/port/user) shQuote 누락 | 태스크 리뷰(`82b7cf7` init 커맨드 직후) | `e07bfc8` |
| Docker 304 미무시(stop), Traefik 이중 등록, logs 멀티플렉스 헤더 미제거 | 최종 리뷰(fix-wave) | `420281e` |
| `parseGitHubRepo` 문자 미제한, `deleteDnsRecord` 타입 필터 없음, write-config/healthz 순서 | 최종 리뷰(fix-wave) | `4bcfe43` |
| stop()이 롤백 보호 try 블록 밖(코드 주석 "재리뷰 지시") | 최종 리뷰(fix-wave) | `dfa8835` |
| Docker 304 미무시(start) | 최종 리뷰(fix-wave) | `2e04ed1` |

## 7. 미검증 영역

| 항목 | 사유/위임 |
|---|---|
| 실제 NAS 대상 SSH/docker 실행 | 자동화 테스트는 러너(runner) 주입으로 대체 — `docs/e2e-checklist.md`로 위임 |
| 실제 Cloudflare API 호출(터널/DNS) | `fetchFn` 주입으로 대체 — `docs/e2e-checklist.md`로 위임 |
| 실제 GitHub(`gh` CLI, Actions 워크플로우 실행) | 러너 주입으로 대체 — `docs/e2e-checklist.md`로 위임 |
| 실제 Docker 데몬 | `docker.test.ts`는 dockerode를 `vi.fn()`으로 목 처리, 실daemon 검증은 리뷰 과정에서 수동 수행 |
| NAS 아키텍처 가정 | `commands/init.ts:108`의 이미지 빌드가 `--platform linux/amd64`로 고정 — amd64 NAS를 전제, arm64 NAS는 미검증 |
