# cloudflared remote-managed tunnel token (hoster init이 발급)
TUNNEL_TOKEN=
# 배포 요청 HMAC 시크릿 (hoster init이 생성, GitHub 레포 시크릿과 동일 값)
HMAC_SECRET=
# ghcr.io pull용 GitHub PAT (read:packages)
GHCR_PAT=
# 기본 도메인
BASE_DOMAIN=example.com
